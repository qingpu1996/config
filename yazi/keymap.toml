# QWERTY branch: keep Yazi's default keybindings.
#
# This file intentionally contains no keymap overrides. It exists so the
# documented symlink path stays valid while avoiding layout-specific remaps.
